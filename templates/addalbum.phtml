<?php require_once 'templates/header.phtml' ?>

<form action="agregardisco" method="POST">
    <div class="form-group">
        <label for="album">Album:</label>
        <input required type="text" class="form-control" name="album" placeholder="Ingrese título del disco">
    </div>
        <label for="dor">Fecha de lanzamiento:</label>
        <input required type="date" class="form-control" name="dor" placeholder="Ingrese fecha de lanzamiento">
    </div>
        <label for="id_artist">Artista:</label>
        <select name="id_artist" class="form-control" placeholder="artista">
            <?php foreach ($artists as $artist) : ?>
                <option value=<?= $artist->id_artist ?>><?= $artist->artist_name ?></option>
            <?php endforeach ?>
        </select>
    </div>
        <label for="duration">Duración:</label>
        <input required type="time" class="form-control" name="duration" placeholder="Ingrese largo del disco">
    </div>
        <button type="submit" class="btn btn-primary mt-2">Agregar album</button>
</form>